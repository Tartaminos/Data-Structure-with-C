// Escreva um programa para encontrar a soma de todos os números naturais de 1 até N usando loops. //

#include <stdio.h>

int main(){
    int entrada, contador, soma1, soma2, controle;

    printf("Digite um número inteiro: ");
    scanf("%d", &entrada);

    contador = 0;
    controle = 0;
    while (contador <= entrada){
        //fazer em casa
    }

    printf("a soma é: %d", soma2);
    return 0;
}